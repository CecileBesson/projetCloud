image: maven:3-openjdk-8
stages:
  - build
  - test-eval-cli
  - sonarqube-check

sonarqube-check:
  stage: sonarqube-check
  image: maven:3.6.3-jdk-11
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"  # Defines the location of the analysis task cache
    GIT_DEPTH: "0"  # Tells git to fetch all the branches of the project, required by the analysis task
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  script:
    - mvn verify sonar:sonar -Dsonar.qualitygate.wait=true -Dsonar.host.url=https://sonarcloud.io
  allow_failure: true
  only:
    - merge_requests
    - master
    - develop

test-eval-cli3bis:
  stage: test-eval-cli
  #only:
  #  - main
  image: ubuntu:latest # node:12.18.1
  before_script:
    - git clone ${repo}
    - cd ${project}
    - npm install
  script:
    - npm run step:3bis
  after_script:
    - cd ..
    - rm -rf ${project}

test-eval-cli2:
  stage: test-eval-cli
  image: ubuntu:latest # node:12.18.1
  #only:
  #  - main
  before_script:
    - git clone ${repo}
    - cd ${project}
    - npm install
  script:
    - npm run step:2
  after_script:
    - cd ..
    - rm -rf ${project}

test-eval-cli3:
  stage: test-eval-cli
  image: ubuntu:latest # node:12.18.1
  #only:
  #  - main
  before_script:
    - git clone ${repo}
    - cd ${project}
    - npm install
  script:
    - npm run step:3
  after_script:
    - cd ..
    - rm -rf ${project}

test-eval-cli1:
  stage: test-eval-cli
  #only:
  #  - main
  image: ubuntu:latest # node:12.18.1
  before_script:
    - git clone ${repo}
    - cd ${project}
    - npm install
  script:
    - npm run step:1
  after_script:
    - cd ..
    - rm -rf ${project}

build:
  stage: build
  script: mvn package
  artifacts:
    paths:
      - target/cloud-0.0.1-SNAPSHOT.jar
